odoo.define('website_order_customer.list_order', function (require) {
    var core = require('web.core');
    var publicWidget = require('web.public.widget');
    var ajax = require('web.ajax');
    var qweb = core.qweb;
    var session = require('web.session');
});
